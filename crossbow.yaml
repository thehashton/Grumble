tasks:

    build:
      adaptor: sh
      command: 'npm run build'

    start:
      adaptor: sh
      command: 'npm-run-all --parallel server start'

    graphql:
      adaptor: sh
      command: 'npm run server'
